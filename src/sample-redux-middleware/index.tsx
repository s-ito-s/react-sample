import { Link } from "react-router-dom"

function SampleReduxMiddleware () {
  return (
    <div>
      <h1>Sample Redux Middleware</h1>
      <div>
        <Link to={`/`}>back</Link>        
      </div>
    </div>    
  )
}

export default SampleReduxMiddleware